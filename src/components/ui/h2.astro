---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h1">;

const { class: className, ...rest } = Astro.props;
---

<h2 class={cn(["text-foreground text-5xl font-bold", className])} {...rest}>
  <slot />
</h2>
