---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"h1">;

const { class: className, ...rest } = Astro.props;
---

<h3 class={cn(["text-foreground text-3xl font-bold", className])} {...rest}>
  <slot />
</h3>
